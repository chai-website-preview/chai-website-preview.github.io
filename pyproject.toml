[project]
name = "chai-astro"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Brian Christian", email = "brian.christian@berkeley.edu" }
]
requires-python = ">=3.12"
dependencies = [
    "markdownify>=1.2.3",
    "pillow>=12.3.0",
    "pyyaml>=6.0.3",
]

[project.scripts]
chai-astro = "chai_astro:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
